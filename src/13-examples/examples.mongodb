use("platzi_store")

// deleting elements from a list with a operator conditional
db.iot.updateMany(
  // query
  {sensor: "A001"},

  //update
  {
    $pull: {
      readings: { $gte: 3 }
    }
  }
)
